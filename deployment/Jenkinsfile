pipeline {
    agent {
        dockerfile true
    }
    stages {
        stage('Build') {
            steps {
                sh '''
			./mvnw -B -DskipTests clean package
			./deployment/appBuild/build.sh
                    '''   
            }
        }
    }
}
